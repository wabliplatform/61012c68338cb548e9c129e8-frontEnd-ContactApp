let apiContactApi = new TempApi.ContactApi();import TempApi from '../src/index';document.getElementById('iti6e').onclick = (event) => {
    event.preventDefault();
    {   location.href= '/homepage' ;}};window.onload = () => {let contactId = window.location.pathname.replace('/contactinfo/','');apiContactApi.getcontact( contactId, (error, data, response) => { if (error) {console.error(error);} else { console.log('API called successfully. Returned data: ' + data); try { document.querySelector('[annotationname = fullname]').textContent = response.body.query.fullname; } catch (e) { console.log(e) };try { document.querySelector('[annotationname = company]').textContent = response.body.query.company; } catch (e) { console.log(e) };try { document.querySelector('[annotationname = comment]').textContent = response.body.query.comment; } catch (e) { console.log(e) };}});};